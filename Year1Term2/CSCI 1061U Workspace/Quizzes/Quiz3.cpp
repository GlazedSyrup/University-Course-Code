// part2++-.cpp
// by Thomas Chiarello
#include <iostream>
#include <fstream>
#include <string>
#include <vector>


int main(){
    std::fstream fs;
    fs.open("excersize-data.bin", std::ios::binary);

    int n;

    if (myFile.is_open()) {
        while (getline(myFile, line)){
            text.push_back(line);
        }
        myFile.close();
    }

    double a [sizeof(fs)];
    for(int i=0; i<n; ++i){

    }
    return 0;

/*

        
    std::cout << text[0];
*/


    return 0;
}